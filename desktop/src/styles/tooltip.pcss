.tooltip {
  position: absolute;
  width: fit-content;
  font-size: small;
  font-weight: 600;
  background-color: var(--overlay);
  padding: 3px 10px;
  border-radius: 4px;
  left: 90%;
  visibility: hidden;
  opacity: 0;
  transition: all 0.2s ease;
  z-index: var(--layer-fg);

  &::before {
    position: absolute;
    content: "";
    width: 10px;
    height: 10px;
    border-radius: 3px;
    background-color: var(--overlay);
    transform: rotate(45deg);
    bottom: 31%;
    right: 87%;
  }
}

.tooltip-wrapper:hover .tooltip {
  visibility: visible;
  opacity: 1;
  left: 100%;
}