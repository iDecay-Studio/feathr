@import "@/shared/styles/anim.pcss" layer(base);
@import "@/shared/styles/themes.pcss" layer(base);
@import "@/shared/styles/components.pcss" layer(components);
@import "@/shared/styles/editor.pcss";
@import "@/shared/styles/cmdbar.pcss";
@import "@/shared/styles/caret.pcss";
@import "@/shared/styles/highlight.pcss";
@import "@/shared/styles/stats.pcss";
@import "@/shared/styles/suggestions.pcss";

:root {
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;

  /*layers*/
  --layer-bg: 0;
  --layer-editor: 2;
  --layer-fg: 5;
  --layer-overlay: 100;
}

*, *::before, *::after {
  box-sizing: border-box;
}

* {
  border-style: none;
  scrollbar-color: var(--scrollbar);
}

html, body, #app {
  width: 100%;
  height: 100%;
  padding: 0;
  margin: 0;
}

html, body {
  overflow: hidden;
  tab-size: 2;

  -webkit-animation: fadeIn 0.5s;
  animation: fadeIn 0.5s;
}